attach(import)

section_id

#ALterar maximo de linhas exibidas

options(max.print = 132000)

section_id

head(section_id)

sort(section_id)

import[33137,3]<-3255

sort(import$section_id)

import[33137,3]

unique(import$section_id)

length(unique(import$section_id))

table(import$section_id)

sort(table(import$section_id))

sort(table(import$course_id))

